# 修复台账（2026-09-21）

对应计划：验收报告 [acceptance-review-2026-09-21.md](acceptance-review-2026-09-21.md) 的「未修缺陷清单」+「测试有效性」+「无法核实」三类。
状态口径：**已修** = 代码已提交且断言能红（做过反向验证）；**已验** = 有可复现的命令输出；**在飞** = 子任务进行中；**未做** = 未开始。

## 一、P1（数据安全 / 离线承诺 / 构建可用）

| ID | 问题 | 状态 | 证据 |
|---|---|---|---|
| FIX-01 | SW 预缓存永不命中（`cache.match` 缺 `ignoreSearch`）、SHELL 漏 `session-model`、`put` 未挂 `waitUntil` | **已修 · 已验** `c92bd38` | `test/sw-shell.test.js` 2 条；反向验证：删掉 session-model → 两条都红并精确点名 `web/index.html → /shared/session-model.js` |
| FIX-02 | SHELL 清单变更与 `VERSION` 不联动（改了清单忘升档 = 静默失效） | **已修 · 已验** `c92bd38` | 清单指纹台账 + `VERSION` 升到 `ww-v14-note`；指纹不一致即红 |
| FIX-03 | `trash()` 先改索引再搬目录、无回滚（**唯一会丢用户数据**） | **已修 · 已验** `c06b0cf` | 改为「先搬数据 → 写 restore.json → 最后动索引」+ 回滚 + 启动对账 `_reconcileTrash()`；`test/profiles-trash-safety.test.js` **5/5**（全部故障注入） |
| FIX-04a | `restoreFromTrash` 生产零调用、无路由暴露 | **已修 · 已验** `8d9f3ac` | `GET /api/profiles/trash` + `POST /api/profiles/trash/<archiveId>/restore`；`test/profiles-restore-route.test.js` 4/4；坑：`/api/profiles/<id>` 兜底只认 UUID，`trash` 会**静默落到 404**，必须排在兜底之前 |
| FIX-04b | 恢复后仍是归档态（`stats` 仍 404），不是「恢复即可用」 | **在飞** | 要求恢复路由内自动取消归档；同时前端回收站入口 |
| FIX-04c | 前端无任何恢复入口 | **在飞** | 双端档案管理弹层加回收区面板 + ui-check 几何断言 |
| FIX-05 | `build-desktop.js` 清理 `dist` 前无守卫 → 干净检出 ENOENT | **已修 · 已验** `d9133ca` | `existsSync` 守卫；`node --check` 通过 |
| FIX-06 | `build-apk.js` 把 JDK/SDK 写死成单机路径 | **已修 · 已验** `d9133ca` | 环境变量 → 常见位置探测；**清空 `JAVA_HOME`/`ANDROID_HOME`/`ANDROID_SDK_ROOT` 后实测仍定位成功** |

## 二、新发现（不在原始清单里）

| ID | 问题 | 状态 | 证据 |
|---|---|---|---|
| NEW-01 | 全量并行下偶发 EPERM：测试把档案根落到全机共享的 `%TEMP%` | **已修 · 已验** `808ce8a` | 探针量化：平铺布局 6 进程×40 次失败 **34–39/40**；独占 `dataDir` 后 **0/240**。连续 4 次全量 630/630 + 我独立复跑 630/630 |
| NEW-02 | `src/api.js` 多处 `this.json(res, e.code \|\| 500)`：fs 的**字符串**错误码被当状态码写出（`status === 'EPERM'`） | **在飞** | 应把非数字 code 归一成 500；新增 `test/error-status-code.test.js` |
| NEW-03 | `%TEMP%\profiles` 累积 1331 个测试残留档案目录 | **已清理 · 已验** `808ce8a` | 清理后残留 0 |
| NEW-04 | `test/remediation.test.js`、`test/engine.test.js` 仍是平铺布局（同类隐患） | **在飞** | 要求改为独占 `dataDir` 并给出「跑完共享目录零新增」的证据 |

## 三、P2 / P3（阶段 2、3，未开始）

| ID | 问题 | 状态 |
|---|---|---|
| FIX-07 | 「清除标注」用 PUT 写空标注而非 DELETE → 导出计数虚高、存储只增不减 | 未做 |
| FIX-08 | Esc 关不掉弹层：`l.hidden = true` 被 `.modal{display:flex}` 覆盖，且不走 `closeModal`（遮罩/焦点不清理） | 未做 |
| FIX-09 | `touch()`/`lastUsedAt` 死字段（排序实为「最近编辑」），且绕过原子写 | 未做 |
| FIX-10 | `saveProfilePrefs` 遇 409 不重拉 → revision 永久过期自锁 | 未做 |
| FIX-11 | `_clearDefaultId()` 从未调用 → 默认档案被删后可能重建「默认玩家」；`_tagSaves` 死形参 | 未做 |
| FIX-12 | `_cleanupStaleTmp()` 无调用者且临时文件命名不匹配 | 未做 |
| FIX-13 | 图标类资源 `max-age=86400` 且 URL 不版本化；`depsUnchanged()` 每次导航全量读盘哈希 | 未做 |
| FIX-14 | `putSync` 绕开每文件串行队列（生产无调用，复用即回归） | 未做 |
| FIX-15 | F5：空刀被拒无可读提示 | 未做 |
| FIX-16 | ui-check 用 `splice` 删掉本段异常 → 全局「无未捕获异常」在该段失效 | 未做 |
| FIX-17 | ui-check 90 条 check 仅 4 处量几何（设置页/恢复卡/横幅/状态条全是「存在+文字」型） | 未做 |
| FIX-18 | ui-check 约 60 处固定 `sleep()`；缺 `pressKey`／离线模拟助手 | 未做 |
| FIX-19 | 剩余恒真/宽容断言（`remediation:269/653/812/1000`、`annotations-delete:84`、`save:54/69`、`keypool:289`） | 部分（`812` 已随验收收紧；`profiles-api` 四处 404/500 已修） |
| FIX-20 | **无机制拦截恒真断言重新进仓库**（防反弹关键） | 未做 |
| FIX-21 | 缺失覆盖：导入近上限成功路径、SW 升级、离线重连、粗粒度性能预算 | 未做 |
| FIX-22 | P3：残留硬编码金色 `#d8b25f`、`i18n.js` 里残留 🐺、证据目录「108 vs 109」口径不一 | 未做 |
| FIX-23 | 交付收敛：升 1.5.3 + 重打三包 + 哈希自动回填 + 台账/证据目录收敛到单一构建号（**需用户确认**） | 未做 |
| FIX-24 | 未核实项补证据：AC-04 双客户端端到端、WebView 导出下载、设备参数与截图语义 | 未做 |

## 四、纪律（每次提交都要满足）

1. 断言只收紧不放宽；新增断言必须做过**反向验证**（改坏 → 必须红 → 改回）。
2. 每个 FIX 一个提交，可单独回滚；提交信息写明「凭什么说修好了」。
3. 阶段收尾跑三绿：`npm test`（当前 630）/ `npm run gate` / `npm run ui:check`。
4. 不碰 `saves/`、正式 `config.json`、不污染 `logs/`；`output/` 下的调试产物用完即删。
5. 阶段 4（打包）**必须等用户点头**。

## 五、当前状态快照

- HEAD：`808ce8a`，与 `origin/main` 同步；工作树干净。
- 测试：**630/630**（连续 4 次全量 + 我方独立 1 次）。
- 已推送的修复：`c92bd38`、`c06b0cf`、`d9133ca`、`8d9f3ac`、`808ce8a`（另加验收阶段 `30989e9`、`d6d44d6`）。
- 在飞：FIX-04b/04c、NEW-02、NEW-04。
