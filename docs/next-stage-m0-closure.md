# M0 结项说明 —— 《AI 狼人杀下一阶段施工计划书》第一阶段

> **依据**：`docs/next-stage-implementation-plan.md` §2（M0 = 当前返修收口 / 工作区交接 / 基线冻结；完成标准 = **现有改动归属明确、数据风险项关闭、全量门禁可复现**）、§2.1（必须先核对的返修项）、§11.3（最终门禁）、§12（每批交付材料）。
> **台账**：`docs/fix-plan-2026-09-21.md` —— 本说明每条状态都能回溯到该文件的编号、提交与反向验证记录。
> **结论**：**M0 完成** ✓。三条完成标准全部成立（§1），§2.1 清单全部有归属（§2），冻结态门禁在干净 worktree 上复现（§4），全部改动已按 FIX 分提交并推送（§3）。**同时明确列出 M0 不宣称的东西**（§6）与需要用户裁决的两件事（§7）。**未做任何打包** ✓。

## 0. 重新登记实际基线（计划书 §1 第 6 行要求）

| 项 | 实际值 | 说明 |
| --- | --- | --- |
| **冻结提交** | **`3128c90`**（已推送，`origin/main` 一致 ✓） | 计划书自述编制基线 `81023b0` 与本轮不同 ⇒ 一律以实际为准 ✓ |
| 全量测试 | **752 / 752 通过 / 0 失败**（`cancelled 0`、`skipped 0`、`todo 0` ⇒ **未执行 = 0**） | 连续三轮同结果 ✓ |
| 工作区 | 干净：仅台账与本说明两处文档改动待提交；代码零改动 | 代码侧 `git diff --numstat` 为空 ✓ |
| 幽灵改动 | `git status` 会对 `src/api.js` 显示 ` M`，但 `git diff --numstat -- src/api.js` **为空**、`ls-files --eol` = `i/lf w/lf` ⇒ **stat-cache 假象，内容零改动** ✓ | 已当场证伪，不记为改动 |
| 版本口径 | 产品 **1.5.2** / Android **versionCode 9**（`version:check` 实测一致 ✓） | 与计划书 §12 的 `1.6.0`/`10`、台账 FIX-23 的 `1.5.3` **冲突 ⇒ 需用户裁决**（§6.2） |
| 打包 | **未做**（APK / Windows 目录包 / EXE 一律未产出、未验证 ✓） | 用户红线，FIX-23 须显式批准 ✓ |
| 台账 | `docs/fix-plan-2026-09-21.md`（本阶段全部编号、裁决、反向验证、三条自我更正） | 无「无主改动」✓ |

## 1. 完成标准逐条判定（计划书 §2 第 33 行）

| 完成标准 | 判定 | 证据 |
| --- | --- | --- |
| ① 现有改动归属明确 | **成立** ✓ | 见 §3。最险处是 `src/api.js` 一个文件里**混着两个任务**的改动（NEW-11 守卫顺序 + 经验池归属）：按 **hunk 内容**（不按行号、不按人名）分类 ⇒ 经验池 3 / NEW-11 5；在隔离 worktree 里分别构造两个中间态并各跑全量：**仅 NEW-11 = 704/704/0**、**再叠加经验池 = 711/711/0**；撤下经验池 hunk 后 `lint` 与 `remediation`（50/50）均 exit 0；恢复后**行尾归一化哈希回到原值** ⇒ 拆分无损。 |
| ② 数据风险项关闭 | **成立** ✓ | 逐项见 §2；硬要求（§2 第 56 行经验池归属）已实现并有 5 例反向验证 ✓ |
| ③ 全量门禁可复现 | **成立** ✓ | 干净 worktree @ `3128c90` 上八条腿实测（§4）；并修掉了一类「门禁不可复现」缺陷本身（NEW-18：主仓库绿、全新检出红）✓ |

## 2. §2.1「必须先核对」逐项判定

| 编号 | 计划书要求 | 判定 | 证据 |
| --- | --- | --- | --- |
| FIX-07 | 标注清除用 DELETE；写入共用串行事务；**不保留绕过队列的写入口** | ✓ | `annotations/store.js:160` 记载「这里曾有 `putSync()`…在每文件串行队列之外」⇒ 绕入口**已删**；串行由 `profiles/store.js:147-149` promise 链互斥；计数判据 `annotations/store.js:58/65`、`api.js:1916/2083` |
| FIX-08/10/15 | 弹层关闭生命周期统一；偏好 409 可恢复；非法操作有可读提示 | ✓ | 前端四项在 `90ff0d6`；**独立验收**在 `13e1026`（空刀提示断言）+ 主控复跑实测 `提示="✗ 请先在「玩家」页点一个座位选出刀口…"` ✓（台账 FIX-15 行原为过期的「在飞」，已改实） |
| FIX-09/11 | `lastUsedAt` 由真实选用/开局更新；默认档案失效**不重复生成**「默认玩家」 | ✓ | `profiles/store.js:219`（创建即落）、`api.js:1078`、`store.js:181-187/444-469`；`migration.js:88/95/103` + `api.js:1723-1730/1777`（含根因与修法） |
| FIX-12/13 | 临时文件清理**真正执行**且识别**实际命名**；静态缓存与内容版本一致 | ✓ | `src/tmp-files.js` 为唯一命名判据；`store.js:128`、`annotations/store.js:145/178`、`migration.js:202` 全走 `tmpPathFor()`；清理真被调用（`api.js:120/167`）+ 启动清扫；缓存侧 `static.js:34/53/184/221` 与测试钩子 `:282` |
| NEW-08 | 导入数量来自**真正落盘**的记录 | ✓ | `872fbf5` + `1b50acb`；主控独立变异（落盘核实改恒真）⇒ 该用例红 ✓ |
| FIX-14 | 删除 `putSync` 旁路 | ✓ | `ea22ea6`；含源码级扫描断言（`putSync` 命中必须为空）⇒ 加回来直接红 |
| FIX-16 | **不得删除页面异常记录**；全局「无未捕获异常」必须重新有效 | **✓ 已修 · 已验 · 已推送** `13e1026`（**仍归 M5 验收**） | 删 `splice` 改只读快照 + 登记表（`max=0` + 精确形态）+「CDP 条数 == 页面 `unhandledrejection` 条数」证明没删记录 + 常驻自证；**主控亲手注入** ⇒ 3 条红、退出码 1、还原后字节一致 ✓ |
| FIX-17 | 几何断言必须量**可见/非零/未遮挡/真实可点击** | **✓ 已修 · 已验 · 已推送** `13e1026`（**仍归 M5 验收**） | `Browser.probe` + `checkGeometry`（宽高/视口相交/中心点命中/遮挡者，失败打印完整实测 JSON）；`realClick` 命中不过则 `NOT_CLICKABLE` 且**不发点击**；顺带挖出 `web/app.js:4176-4184` 令旧断言**空转**的真问题并改用真实场景 |
| FIX-18 | 固定 sleep 改条件等待；无法条件化的**逐条给理由** | **✓ 已修 · 已验 · 已推送** `13e1026`（**仍归 M5 验收**） | 可执行 `await sleep()` **100 → 9**（2 处为助手自身轮询）⇒ 保留 **7 处**并逐条说明；新增 `waitFor`/`waitCheck`/`waitExpr` 与 CDP 离线助手（实测 `fetch` 真失败、横幅 4ms 自动出现/消失） |
| FIX-19 | 恒真/宽容断言清理后基线只收缩 | ✓ | `a799fdb` + `a0d6d77`；8 个真实源码变异全部变红，且同一观测值下**旧断言判定为真**（证明旧写法确实放过回归）；主控独立核验 49/49 与两次变异各自红 ✓ |
| FIX-20 | 无机制拦截恒真断言重新进仓库 | ✓ | `3cc42b4`；推送钩子实测跑起来并放行（`✓ [pre-push] 全量测试通过`） |
| FIX-21 | 缺失覆盖补齐 | ✓ `ef3a4ad`（**仍归 M5 验收**） | 13 条，四项反向验证各自能红；诚实边界：性能阈值余量 14–27 倍 |
| FIX-22 | 视觉残留归 **M1** 收口 | 未动 ✓（按计划书） | M1 未获授权 ⇒ 不动 ✓ |
| FIX-23 | 版本/构建号/包哈希归 **M6**；打包需用户批准 | 未动 ✓ | 未打包；版本口径冲突待裁决 ✓ |
| FIX-24 | 未核实项补证据（AC-04、WebView 导出、设备参数） | 未动 ✓（**仍归 M5 验收**） | 台账保留该行，未从台账消失 ✓ |
| §2 第 56 行 | **经验池必须固定归属原 UUID**；切默认/归档/重建不得被他人继承；如需迁移必须备份、原子写、校验、幂等游标 | **✓ 关闭** `19f45e8` | 归属改由**首次落盘即冻结的档案 UUID** 唯一决定（与「当前默认标记」、昵称无关）；**零拷贝**（未触发「必须备份」前提）；同目录临时文件 + 原子替换 + **写后读回校验** + **幂等游标**；**5 例语义注入全红**（归属键改回默认标记 / 重建默认档案顺手接管 / 按昵称判定 / 标记不冻结 / 升级采纳每次同步） |

## 3. 工作区交接与提交清单（M0 完成标准第①条）

**本阶段入库并推送的提交**（按时间序，全部已推送）：

| 提交 | 内容 |
| --- | --- |
| `f5cc36f` | NEW-18：补钉两份 `web/assets` 派生副本为 LF —— 修掉「主仓库绿、全新检出红」的门禁不可复现 |
| `ad7c400` | NEW-15：测试临时目录泄漏 38 处（9 文件 + 夹具）⇒ 单次全量残留 38 → 0 |
| `df37805` | NEW-11：守卫顺序统一 —— 未授权调用不再用 409 与业务文案泄露局面 |
| `19f45e8` | 经验池归属钉死在原 UUID（§2 第 56 行硬要求） |
| `81fdc07` | 品牌字节哈希路径的行尾钉版覆盖元断言（防「主仓库绿、干净检出红」复发） |
| `524dbd0` | 台账 M0 收口（五个新问题 + 冻结态门禁 + 三条更正） |
| `13e1026` | FIX-16/17/18：恢复全局异常断言牙齿、几何断言量真值、固定等待改条件等待 |
| `bca5891` | NEW-17：12 个测试文件不再写共享路径 + 新增静态/动态隔离守卫 |
| `3128c90` | 覆盖率回归修复：行尾钉版判据拆成参数化模块（**冻结提交**） |
| 本次 | 台账收口 + 本结项说明 |

更早阶段（阶段 1/2 与计划书本身）：`d26c947`、`872fbf5`、`1b50acb`、`de9652a`、`b5bc9d8`、`1a702be`、`a0d6d77`、`b8c7e04`、`f8a8543`、`90ff0d6`、`b2e04ba`。

**归属纪律**：每个提交只含其自身任务的明确路径（绝不 `git add -A`）；两个任务共用 `src/api.js` 时按 hunk 内容分类、并在隔离 worktree 里验证每个中间态各自全绿后才提交。

## 4. 冻结态门禁（计划书 §11.3）

**在干净 worktree @ `3128c90` 上实跑**（`ui:check` 在主仓库跑，其代码与该提交字节一致、经 `git hash-object` 校验 ✓）：

| 腿 | 命令 | 结果 | 通过 / 失败 / 未执行 |
| --- | --- | --- | --- |
| 全量测试 | `node --test`（**连续三轮**） | **exit 0** ×3 | **752 / 0 / 0** ×3（`cancelled 0`、`skipped 0`、`todo 0`） |
| 覆盖率 | `npm run coverage` | **exit 0** | 行 **94.44%**（92）、分支 **86.91%**（84）、函数 **88.88%**（86）；api.js 两项亦过 |
| 静态检查 | `node scripts/lint.js` | **exit 0** | 181 文件 × 9 规则 |
| eslint | `npx --no-install eslint .` | **exit 0** | 替代 `npm run eslint`：本机 `node_modules` 为空（0 个顶层项）⇒ 无本地 bin，**主仓库亦如此**（非 worktree 问题）；eslint 版本与配置未变，**不是放宽判据** |
| 对局评估 | `npm run eval` | **exit 0** | 全部指标在容差内（30 局） |
| 品牌资产 | `npm run brand:check` | **exit 0** | **70 项通过 / 0 项失败**（含行尾钉版覆盖一节；**全新检出同结论** ✓） |
| 版本一致性 | `npm run version:check` | **exit 0** | `1.5.2` / `9` |
| 防忘记守卫 | `npm run guards` | **exit 0** | 违规 0 处（基线内 0 / 基线外新增 0） |
| UI 几何回归 | `npm run ui:check -- --full --strict`（**连续两次**） | **exit 0** ×2（60.3s / 60.0s） | 全部规划段落执行且无失败。**退出语义（源码 `:20-24`）**：0 = 全执行无失败；**2 = 有「未执行」项**（哪怕无失败）；1 = 有执行且失败 ⇒ 计划书「必测未执行不能返回成功」由 **exit 2** 落实，故本条必须 exit 0 ✓（两次均未出现 2） |
| 制品验收 | `npm run app:verify -- --release` | **未执行** | §11.3 属「冻结提交后执行」，但 §12/§39 明确制品与三包重建属 **M6**；M0 不产出制品且打包须用户批准 ⇒ 如实记为未执行并归属 M6，**不以「跳过」冒充通过** ✗ |

**附加验证**：私有 TEMP 沙箱下 `node --test` 连续三轮 ⇒ 沙箱顶层残留 **0 项** ✓（NEW-15 修复的性质在冻结态复现）。

## 5. 逐项反向验证（含主控亲手注入）

| 项 | 注入 / 反证 | 结果 |
| --- | --- | --- |
| 经验池归属 | 5 种语义注入（归属键改回「当前默认标记」/ 重建默认档案顺手接管 / 按昵称判定 / 标记不冻结 / 升级采纳改每次同步） | 各自命中预期断言 ⇒ **全红** ✓；另 7 条覆盖归档/回收站/恢复/切默认/重启/重建/改名重名/失败关闭 |
| NEW-11 守卫顺序 | 4 例注入（含「四处全改回旧顺序跑全量」）+ 主控反证（先跑未打补丁版本 ⇒ 2 条新断言当场红） | 前三例各命中预期断言；第四例证明全仓只有新增护栏依赖该顺序；主控反证证明断言**非恒真** ✓ |
| NEW-15 临时目录泄漏 | 私有 TEMP 沙箱前后差 + 失败路径注入 + 删除必然失败（只告警不把绿用例改红） | 修前单次全量残留 **38** 个顶层目录（9 文件 23 个命名族）；修后 **0**（主控独立复测：全量绿 + 沙箱残留 0）✓；断言一行未动 |
| NEW-18 门禁不可复现 | 在**全新工作树**复现（CRLF 检出 ⇒ 两份派生副本 7659B/68CR、72CR，SHA-256 与源不符 ⇒ exit 1）；修复后同一干净检出转绿 | 过程诚实记录：第一次只补一份、复验**仍红**，才发现第二份漏网 ✓ |
| NEW-19（防复发元断言） | 4 例注入（含「注释掉钉版行而工作区字节不变」）+ 主控另换一条路径独立复现 | 既有 62 项检查全绿、**只有新断言红**并指名缺哪条规则 ✓（该守卫已交付，见 §6.5） |
| NEW-17 测试隔离 | 主控在**用完即弃的 worktree** 里摘掉 `keypool.test.js` 的一行 `saveDir:` | 守卫 4 条中 **2 条红**：静态那条**指名 `keypool.test.js:295`**、动态那条同时报出根目录多出数据目录 ⇒ 两类判据都真有牙齿 ✓ |
| FIX-16 异常断言 | 主控亲手注入一条**预期外**未捕获异常 | 本段 2 条 + 全局 1 条共 **3 条红**，全局断言**点名说出注入的消息**，退出码 **1**（不是 2）；`git checkout --` 还原后工作区 blob 与入库 blob **同为 `1f57e446…`**、零残留 ✓ |
| 覆盖率回归 | 行级诊断（`NODE_V8_COVERAGE`）取证：`check-brand-assets.js` 835 行中 **386 行未执行 = 53.77%**，与门禁自报 55.88% 吻合 | 根因定性为「分母被抬高」（新测试 require 了 CLI 脚本），修法是拆成**参数化模块**使两个脚本文件干净退出分母；修复后该模块 **538/538 行 = 100%** ✓ |

## 6. 诚实边界（M0 不宣称的东西）

1. **不宣称整个下一阶段完成**。计划书 §13 的六条最终完成定义（多档案与自定义头像、双端首页/玩家中心/三步开局/局中标注/笔记/结算、导出导入、三包一致…）**一条都还没开始** ⇒ M1 及之后均未获授权 ✓。
2. **版本口径未决**：`1.5.2`/`9`（实际）、`1.5.3`（台账 FIX-23）、`1.6.0`/`10`（计划书 §12）三者不一致 ⇒ **需用户裁决**，M0 未擅自改动任何版本号 ✓。
3. **未打包**：APK / Windows 目录包 / EXE 一律未产出、未验证。`release/` 里只有 1.4 / 1.5.0 / 1.5.2 的历史制品（2026-09-17 ~ 09-20），**不得**当本轮证据。
4. **FIX-16/17/18/21/24 按计划书仍归 M5 验收** ⇒ 本阶段只交付代码与验证，台账与本文档**均不写「关闭」** ✓。
5. **暂缓项（已登记，非 M0 阻断）**：NEW-16（未授权者仍可用 404 vs 403 判定存档是否存在 —— 改它要动对外 404 语义与前端契约）；NEW-19 的**第二半**（私有沙箱残留量作为门禁腿；其第一半「静态基线守卫」已随 NEW-17 交付 ✓）；5 个用 `process.on('exit')` 清理的测试文件（实测 0 残留，登记为隐性债）。
6. **一处未定位的极小残留**：八腿连跑那一次，私有沙箱顶层残留 **1 项**；`node --test` 连续三轮均为 **0 项**（已复测）⇒ 残留来自测试套件之外的某条腿（未定位是 `coverage` 还是 `eval`）。如实登记，不假装不存在。
7. **NEW-17 的能力边界**：它证明了「共享写点 + 并发抢写 + 污染检出」这一类机制已被消除；但主控先前观察到的 **3 条并发假红，该任务修复前也未能复现** ⇒ **未定位那 3 条的近因**，不宣称「任何负载下都无抖动型假红」。
8. **一条被实测推翻的主控前提**：我原先断言「子进程的覆盖率不会并回父进程」，钉版任务按此写了 subprocess 用例后**当场看见两个脚本文件回到覆盖表**（74.86% / 95.35%）⇒ 该前提**错误**，用例已弃用，且**没有**用「关掉覆盖率收集」之类手段掩盖（那才是放水）。端到端结论由 `brand:check` 腿承担。
9. **经验池修复的边界**：无归属或非 UUID 归属的对局仍读写旧池（迁移失败降级路径）；`stats()` 仍报旧池全局口径；经验内容无归属戳 ⇒ **此前已被旧缺陷混入的历史无法回溯归属**；标记在单进程内只读一次；「回收站里的档案仍按 UUID 读自己的池」是**选定语义**而非缺陷。
10. **门禁可复现的证明方式**：仓库**配了** CI 工作流（`.github/workflows/ci.yml`），但本阶段的证据**不是**来自 CI，而是**本机干净检出实跑** ✓。此前台账里一句「CI 因免费额度不可用」属**未经核验的说法**，以此处为准。
11. **UI 验收的能力边界**（由 `ui-check` 作者自述并经主控复核）：不覆盖颜色/对比度、字号可读性、动画中途态、语义错误的层叠、真机触摸；移动端只验 `Emulation.setDeviceMetricsOverride` 的视口。

## 7. 下一步（等用户裁决 / 授权）

1. **用户裁决**：版本号口径（`1.5.2`/`1.5.3`/`1.6.0` 与 `versionCode` 9/10）。
2. **用户授权 M1**（视觉 token、共享状态、基础控件、自定义头像数据层；**FIX-22 也归 M1**）。
3. **FIX-23 打包**：须用户显式批准后才动（届时属 M6，含三包重建与哈希回填）。
