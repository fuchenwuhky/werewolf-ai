# 行尾固定（只这四份，故意不用 * text=auto —— 那会引发全仓重新规范化并污染并行工作区）
#
# 为什么需要：这四份文件的**字节**被测试用 SHA-256 钉住 ——
#   · web/index.html、web/m/index.html 的内联 <script> 哈希，钉在 src/static.js 的 CSP 白名单里；
#   · design/brand/v2/wolf-emblem.svg 的哈希，钉在品牌 manifest 的 sourceSha256；
#   · design/brand/v2/export/app-icon.svg 的哈希，钉在品牌 manifest 的 files[] 里。
# 而 Windows 默认 core.autocrlf=true，检出会变成 CRLF，于是本地这两条守卫假红、
# CI（Linux，LF）却是绿的 —— 2026-09-21 真的把主控误导过一次（差点去"重新 pin"本来正确的值）。
# 固定为 LF 后，Windows 检出与 CI 看到的字节一致，这类假红永久消失。
#
# 注意：manifest 里另外 21 个 PNG + 1 个 ICO 是**二进制**，git 不会做行尾转换，
# 哈希与行尾无关，因此**不要**把它们写进来。
design/brand/v2/wolf-emblem.svg text eol=lf
design/brand/v2/export/app-icon.svg text eol=lf
web/index.html text eol=lf
web/m/index.html text eol=lf
